{% extends "base.twig" %}

{% block content %}

{% if not admin %}
<div>
    <h1 class="display-4  text-center pt-2">Портал о незаконных свалках</h1>
    <h3 class="text-center"><small class="text-muted text-center">В России перерабатывается только 4% мусора.</small></h3>
    <hr class="my-4">
    <h5 class="text-center pt-2" width="75%">Пишите о незаконных свалках, сообщайте в органы власти, учавствуйте в волонтерских уборках, отслеживайте события свалок.
        Да прибудет с нами чистота.
    </h5>
</div>
{% endif %}

<div class="row justify-content-center">
    <div class="container pt-5">
        <div class="card-columns" style="display: inline-block">
            {% for lf in landfill %}
                <div class="card bg-light border-secondary text-center"  style="min-width: 15rem;">
                    {% if (lf.photos)|length>0  %}
                        <img src="{{ lf.photos[0] }}" class="card-img-top ">
                    {% endif %}
                    <div class="card-body ">
                        <h5 class="card-title">{{ lf.name }}</h5>
                        <p class="card-text">Дата открытия: {{ lf.dateFind }}</p>
                        <a href="/landfill/{{ lf.id }}" class="btn btn-info btn-sm btn-card">Открыть польностью</a>
                        
                        {% if admin %}
                        <div class="actions">
                            <button type="button" class="btn btn-primary btn-sm btn-card" onclick="landfill.edit({{ lf.id }})">Изменить</button>
                            <button type="button" class="btn btn-danger btn-sm btn-card" onclick="landfill.del({{ lf.id }})">Удалить</button>
                        </div>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}